[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  # Set this in Netlify UI to your backend, e.g. https://<render>.onrender.com/api
  # VITE_BASE_API = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
